# Relatório de QA — Issue #173

## Resumo
- **Status:** APROVADO
- **Data:** 2026-07-15 17:15:22

## Acceptance Criteria
| Critério | Status | Observação |
|----------|--------|------------|
| AC1 — Code como string obrigatória, max 50 chars | Aprovado | Mapping: NVARCHAR(50), HasMaxLength(50), IsRequired(). Propriedade C# como string? (nullable) para compatibilidade com construtor legacy. |
| AC2 — FileTypeTranslation com FK e unique (FileTypeId, LanguageCode) | Aprovado | PK composta (FileTypeId, LanguageCode) garante unicidade. FK configurada em FileTypeMapping com cascade delete. |
| AC3 — Unique index UX_FileTypes_Code | Aprovado | HasIndex(Code).IsUnique().HasFilter("[IsDeleted] = 0").HasDatabaseName("UX_FileTypes_Code") |

## Testes Técnicos
| Comando | Status | Observação |
|---------|--------|------------|
| dotnet build | Passou | 0 erros, 1359 warnings (todos preexistentes) |
| dotnet test | Passou | 31/31 testes passaram, 0 falhas, 0 ignorados |

## Pontos de Atenção Verificados

### FileTypeEntity
- [x] Code adicionado como nova propriedade (além de MimeType e Extension)
- [x] Construtor legacy de 3 parâmetros preservado — delega para o novo construtor de 4 parâmetros com code: null
- [x] Navigation property Translations adicionada (ICollection<FileTypeTranslationEntity>)
- [x] Método UpdateCode criado para atualização do Code
- [x] Private setters e construtor protegido para EF Core — convenções DDD mantidas

### FileTypeTranslationEntity
- [x] FK FileTypeId para FileTypeEntity
- [x] PK composta (FileTypeId, LanguageCode) — unique garantido
- [x] Propriedades: LanguageCode (NVARCHAR(10)), Name (NVARCHAR(200)), Description (NVARCHAR(500))
- [x] Constraint nomeada UQ_FileTypeTranslations_FileType_Language adicional (requerida pelo schema SQL)

### FileTypeMapping
- [x] Mapeamento de Code como NVARCHAR(50) obrigatório
- [x] FK configurada para FileTypeTranslations com cascade delete
- [x] Unique index UX_FileTypes_Code com filtro WHERE IsDeleted = 0

### FileTypeTranslationMapping
- [x] PK composta mapeada
- [x] Unique index UQ_FileTypeTranslations_Language_Name para nomes únicos por idioma

## Observações
- **Propriedades nullable (string?):** Code em FileTypeEntity e LanguageCode/Name/Description em FileTypeTranslationEntity estão declaradas como string? (nullable reference type), enquanto os mappings usam .IsRequired(). Isto é intencional para suportar o construtor legacy e é consistente com outras entidades do projeto. A base de dados irá enforced NOT NULL.
- **DocumentType:** O branch contém também ficheiros novos para DocumentTypeEntity/DocumentTypeTranslationEntity (issue #172). Estes não fazem parte do escopo desta issue mas não afetam a validação.
- Nenhum teste existente foi removido ou desabilitado.
- Nenhum endpoint, contrato ou interceptor foi alterado.

## Bugs Encontrados
Nenhum.

## Decisão Final
- **APROVADO:** seguir para For Deploy.
